[build]
  base = "."
  publish = "build"
  command = "npm run build"

# SPA rewrite for React Router (serves index.html for all routes)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[context.production.environment]
  NODE_ENV = "production"
